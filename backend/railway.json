{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE"
  },
  "deploy": {
    "startCommand": "gunicorn \"app:create_app()\" --bind 0.0.0.0:${PORT:-5000} --workers 2 --timeout 600",
    "restartPolicyType": "ON_FAILURE"
  }
}
