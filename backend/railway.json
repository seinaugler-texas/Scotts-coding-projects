{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "sh -c 'gunicorn \"app:create_app()\" --bind 0.0.0.0:${PORT:-5000} --workers 2 --timeout 120'",
    "restartPolicyType": "ON_FAILURE"
  }
}
